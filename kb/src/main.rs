
//use crate::knowledge_base::KnowledgeBase;  this is giving us errors
#![allow(unused_imports)]
use std::rc::Rc;
use crate::fact::Fact;
use crate::rule::Rule;

fn main() {
    println!("Hello, world!");
}
